"""Built-in reimap plugins.

Each module here exposes ``register(hooks, runtime)`` and serves both as useful
functionality and as a worked example of the hook API. Copy any of them into your
``<config>/plugins/`` directory as a starting point for your own plugin.
"""
